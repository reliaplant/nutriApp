'use client'

// Componente reemplazado por FoodPreferenceList (listado único con like/dislike).
// Se deja un stub vacío porque el entorno no permite borrar el archivo.

const FoodPreferenceChips = () => null;

export default FoodPreferenceChips;
